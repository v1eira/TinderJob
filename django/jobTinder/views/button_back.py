from .button_command import button_command


class button_back(button_command):
    # usuario e vaga viriam de template

    def executar(self):
        origem = usuario.nome  # não implementado ainda
        vaga = vaga.nome  # não implementado ainda
        back()